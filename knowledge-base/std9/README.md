# knowledge-base/std9

Place legally usable Gujarat Board (GSEB) Std. 9 study material here.

Supported formats: **PDF, TXT, MD, DOCX**

```bash
cd backend
python -m scripts.import_documents --dir ../knowledge-base/std9 --standard 9 \
  --subject Science --source-type curated
```
